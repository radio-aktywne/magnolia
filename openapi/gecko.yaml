openapi: "3.1.0"
info:
  title: gecko
  version: "0.26.0"
paths:
  /ping:
    get:
      tags:
        - Ping
      summary: Ping
      description: Ping.
      operationId: PingPing
      responses:
        "204":
          description: Request fulfilled, nothing follows
          headers:
            Cache-Control:
              schema:
                type: string
              required: true
              deprecated: false
      deprecated: false
    head:
      tags:
        - Ping
      summary: Ping headers
      description: Ping headers.
      operationId: PingHeadping
      responses:
        "204":
          description: Request fulfilled, nothing follows
          headers:
            Cache-Control:
              schema:
                type: string
              required: true
              deprecated: false
      deprecated: false
  /recordings/{event}/{start}:
    get:
      tags:
        - Recordings
      summary: Download recording
      description: Download a recording.
      operationId: RecordingsEventStartDownload
      parameters:
        - name: event
          in: path
          schema:
            $ref: "#/components/schemas/DownloadRequestEvent"
          required: true
          deprecated: false
        - name: start
          in: path
          schema:
            $ref: "#/components/schemas/DownloadRequestStart"
          description: Datetime without timezone.
          required: true
          deprecated: false
      responses:
        "200":
          description: Stream Response
          headers:
            Last-Modified:
              required: true
              deprecated: false
            Content-Length:
              required: true
              deprecated: false
            ETag:
              required: true
              deprecated: false
            Content-Type:
              required: true
              deprecated: false
          content:
            "*/*":
              schema:
                type: string
                format: binary
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
      deprecated: false
    put:
      tags:
        - Recordings
      summary: Upload recording
      description: Upload a recording.
      operationId: RecordingsEventStartUpload
      parameters:
        - name: event
          in: path
          schema:
            $ref: "#/components/schemas/UploadRequestEvent"
          required: true
          deprecated: false
        - name: start
          in: path
          schema:
            $ref: "#/components/schemas/UploadRequestStart"
          description: Datetime without timezone.
          required: true
          deprecated: false
        - name: Content-Type
          in: header
          schema:
            anyOf:
              - $ref: "#/components/schemas/UploadRequestType"
              - type: string
                contentMediaType: application/json
                contentSchema:
                  $ref: "#/components/schemas/UploadRequestType"
            title: UploadRequestType
            description: Type of the recording data.
          description: Type of the recording data.
          required: true
          deprecated: false
      requestBody:
        content:
          "*/*":
            schema:
              type: string
              format: binary
        required: true
      responses:
        "204":
          description: Request fulfilled, nothing follows
          headers: {}
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
      deprecated: false
    delete:
      tags:
        - Recordings
      summary: Delete recording
      description: Delete a recording.
      operationId: RecordingsEventStartDelete
      parameters:
        - name: event
          in: path
          schema:
            $ref: "#/components/schemas/DeleteRequestEvent"
          required: true
          deprecated: false
        - name: start
          in: path
          schema:
            $ref: "#/components/schemas/DeleteRequestStart"
          description: Datetime without timezone.
          required: true
          deprecated: false
      responses:
        "204":
          description: Request fulfilled, nothing follows
          headers: {}
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
      deprecated: false
    head:
      tags:
        - Recordings
      summary: Download recording headers
      description: Download recording headers.
      operationId: RecordingsEventStartHeaddownload
      parameters:
        - name: event
          in: path
          schema:
            $ref: "#/components/schemas/HeadDownloadRequestEvent"
          required: true
          deprecated: false
        - name: start
          in: path
          schema:
            $ref: "#/components/schemas/HeadDownloadRequestStart"
          description: Datetime without timezone.
          required: true
          deprecated: false
      responses:
        "200":
          description: Request fulfilled, headers follow
          headers:
            Last-Modified:
              required: true
              deprecated: false
            Content-Length:
              required: true
              deprecated: false
            ETag:
              required: true
              deprecated: false
            Content-Type:
              required: true
              deprecated: false
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
        "404":
          description: Nothing matches the given URI
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Not Found Exception
                examples:
                  - status_code: 404
                    detail: Not Found
                    extra: {}
      deprecated: false
  /recordings/{event}:
    get:
      tags:
        - Recordings
      summary: List recordings
      description: List recordings.
      operationId: RecordingsEventList
      parameters:
        - name: event
          in: path
          schema:
            $ref: "#/components/schemas/ListRequestEvent"
          required: true
          deprecated: false
        - name: after
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/ListRequestAfter"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/ListRequestAfter"
                title: ListRequestAfter
              - type: "null"
            description: >-
              Only list recordings after this datetime (in event timezone).
          description: >-
            Only list recordings after this datetime (in event timezone).
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: before
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/ListRequestBefore"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/ListRequestBefore"
                title: ListRequestBefore
              - type: "null"
            description: >-
              Only list recordings before this datetime (in event timezone).
          description: >-
            Only list recordings before this datetime (in event timezone).
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: limit
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/ListRequestLimit"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/ListRequestLimit"
                title: ListRequestLimit
              - type: "null"
            description: >-
              Maximum number of recordings to return. Default is 10.
          description: Maximum number of recordings to return. Default is 10.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: offset
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/ListRequestOffset"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/ListRequestOffset"
                title: ListRequestOffset
              - type: "null"
            description: Number of recordings to skip.
          description: Number of recordings to skip.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
        - name: order
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/ListRequestOrder"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/ListRequestOrder"
                title: ListRequestOrder
              - type: "null"
            description: Order to apply to the results.
          description: Order to apply to the results.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListResponseResults"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
      deprecated: false
  /sse:
    get:
      tags:
        - SSE
      summary: Get SSE stream
      description: Get a stream of Server-Sent Events.
      operationId: SseSubscribe
      parameters:
        - name: types
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/SubscribeRequestTypes"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/SubscribeRequestTypes"
                title: SubscribeRequestTypes
              - type: "null"
            description: Types of events to subscribe to.
          description: Types of events to subscribe to.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, stream of Server-Sent Events follows
          headers:
            Cache-Control:
              schema:
                type: string
              required: true
              deprecated: false
            Connection:
              schema:
                type: string
              required: true
              deprecated: false
          content:
            text/event-stream:
              schema:
                type: string
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
      deprecated: false
  /test:
    get:
      tags:
        - Test
      summary: Test
      description: Test.
      operationId: TestTest
      parameters:
        - name: parameters
          in: query
          schema:
            oneOf:
              - anyOf:
                  - $ref: "#/components/schemas/TestRequestParameters"
                  - type: string
                    contentMediaType: application/json
                    contentSchema:
                      $ref: "#/components/schemas/TestRequestParameters"
                title: TestRequestParameters
              - type: "null"
            description: Parameters for testing.
          description: Parameters for testing.
          required: false
          deprecated: false
          allowEmptyValue: false
          allowReserved: false
      responses:
        "200":
          description: Request fulfilled, document follows
          headers: {}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestResponseResult"
        "400":
          description: Bad request syntax or unsupported method
          content:
            application/json:
              schema:
                properties:
                  status_code:
                    type: integer
                  detail:
                    type: string
                  extra:
                    additionalProperties: {}
                    type:
                      - "null"
                      - object
                      - array
                type: object
                required:
                  - detail
                  - status_code
                description: Validation Exception
                examples:
                  - status_code: 400
                    detail: Bad Request
                    extra: {}
      deprecated: false
components:
  schemas:
    DeleteRequestEvent:
      type: string
      format: uuid
    DeleteRequestStart:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    DownloadRequestEvent:
      type: string
      format: uuid
    DownloadRequestStart:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    EventType:
      type: string
      enum:
        - test
      title: EventType
      description: Event types.
    HeadDownloadRequestEvent:
      type: string
      format: uuid
    HeadDownloadRequestStart:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    ListOrder:
      type: string
      enum:
        - asc
        - desc
      title: ListOrder
      description: Order to list recordings.
    ListRequestAfter:
      anyOf:
        - $ref: "#/components/schemas/NaiveDatetime"
        - type: "null"
    ListRequestBefore:
      anyOf:
        - $ref: "#/components/schemas/NaiveDatetime"
        - type: "null"
    ListRequestEvent:
      type: string
      format: uuid
    ListRequestLimit:
      anyOf:
        - type: integer
        - type: "null"
    ListRequestOffset:
      anyOf:
        - type: integer
        - type: "null"
    ListRequestOrder:
      anyOf:
        - $ref: "#/components/schemas/ListOrder"
        - type: "null"
    ListResponseResults:
      properties:
        count:
          type: integer
          title: Count
          description: Total number of recordings that match the request.
        limit:
          anyOf:
            - type: integer
            - type: "null"
          title: Limit
          description: Maximum number of returned recordings.
        offset:
          anyOf:
            - type: integer
            - type: "null"
          title: Offset
          description: Number of skipped recordings.
        recordings:
          items:
            $ref: "#/components/schemas/Recording"
          type: array
          title: Recordings
          description: List of recordings.
      type: object
      required:
        - count
        - limit
        - offset
        - recordings
      title: RecordingList
      description: List of recordings.
    NaiveDatetime:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    Parameters:
      properties:
        message:
          anyOf:
            - type: string
            - type: "null"
          title: Message
          description: Optional message to include in the test.
          default: null
      type: object
      title: Parameters
      description: Parameters for testing.
    Recording:
      properties:
        event:
          type: string
          format: uuid
          title: Event
          description: Identifier of the event.
        start:
          $ref: "#/components/schemas/NaiveDatetime"
      type: object
      required:
        - event
        - start
      title: Recording
      description: Recording data.
    SubscribeRequestTypes:
      anyOf:
        - type: string
        - items:
            $ref: "#/components/schemas/EventType"
          type: array
          uniqueItems: true
        - type: "null"
    TestRequestParameters:
      anyOf:
        - $ref: "#/components/schemas/Parameters"
        - type: "null"
    TestResponseResult:
      properties:
        success:
          type: boolean
          title: Success
          description: Indicates if the test was successful.
        message:
          type: string
          title: Message
          description: Message returned from the test.
      type: object
      required:
        - success
        - message
      title: Result
      description: Result of the test.
    UploadRequestEvent:
      type: string
      format: uuid
    UploadRequestStart:
      type: string
      format: date-time
      description: Datetime without timezone.
      examples:
        - 2000-01-01T00:00:00
    UploadRequestType:
      type: string
